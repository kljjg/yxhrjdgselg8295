apiVersion: v1
kind: Service
metadata:
  name: yzlnrjdk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yzlnrjdk"
    port: 8080
  selector:
    app: yzlnrjdk
